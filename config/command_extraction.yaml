mach:
  mode: savgol_mach
  tol: 0.01
  min_len: 120
  alt_threshold: 20000
  use_alt: true
  smooth_window: 200
  smooth_method: binned

mach_regime:
  min_alt_ft: 28000
  bridge_s: 600

cas:
  mode: savgol_cas
  tol: 2.5
  min_len: 60
  use_alt: false
  smooth_window: 150
  smooth_method: binned

vz:
  mode: savgol_vz
  tol: 25
  min_len: 15
  use_alt: false
  min_abs_value: -1
  smooth_window: 51
  smooth_method: savgol
  fill:
    enabled: true
    bridge_gaps: true
    max_gap_fill_s: 120
    fill_gaps: true
    ramp_s: 1.0
    change_tol_fpm: 50.0

h_sel:
  mode: bilateral_vz
  sigma_s: 4
  sigma_r: 250
  n_passes: 2
  tol_ftmin: 250
  min_len: 10
